# Project Context

Building a local Claude Code workspace integrated with Notion via MCP.
Core pipeline: Read Notion pages via MCP and save locally as .txt files. Includes AI-assisted transformations.

## GSD Configuration

- Framework: get-shit-done-cc (GSD)
- Budget Profile: `eco` (Strictly adhere to this to prevent token exhaustion).

## Strict Rules

1. ZERO COMMENTS IN CODE. Not a single line. No exceptions.
2. All documentation (README.md) must be auto-generated by the `/generate-docs` skill.
3. No hardcoded paths or IDs in skills.
